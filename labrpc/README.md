Empty — copy from mit-6.5840/src/labrpc/. Simulated RPC layer that
lets the tester delay, drop, and reorder messages to simulate real
network failures. This is what makes the fault-injection tests possible.
