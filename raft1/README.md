Empty — copy from mit-6.5840/src/raft1/ (see top-level README).
This is where Make(), Start(), GetState(), RequestVote/AppendEntries
RPC handlers, persist()/readPersist(), and Snapshot() live.
See docs/PROJECT_PLAN.md phases 2-5.
